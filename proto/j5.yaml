registry:
  owner: pentops
  name: messaging

packages:
  - name: o5.messaging.v1

options:
  subPackages:
    - name: service
    - name: topic

dependencies:
  - bufRegistry:
      owner: bufbuild
      name: protovalidate


